# 岗位匹配与排序评分规则

## 1. 评分目标

岗位推荐分为两层：

1. **基础适配分（Match Score）**：衡量用户的技能和项目经历是否与岗位本身匹配。
2. **最终排序分（Rank Score）**：在基础适配分基础上，叠加用户当前的岗位方向与通勤偏好，用于多个候选岗位之间的排序。

城市、学历和岗位类型属于硬约束，在召回前过滤，不参与后续评分。

------

## 2. 硬约束过滤

以下条件在岗位召回前通过 `where` 过滤：

- 城市；
- 学历；
- 岗位类型：实习、校招、社招，默认实习。

若岗位库中没有满足全部硬约束的岗位，应直接说明，不自动放宽条件。

JD 未明确学历要求或用户学历信息无法判断时，不直接过滤，但在报告中标记为待确认。

------

## 3. 基础适配分

```text
match_score = skill_score × 0.55 + project_score × 0.45
```

基础适配分用于回答：该岗位是否与用户已有能力和经历匹配。

### 3.1 技能分：规则计算

技能分由 JD 的必备技能、优先技能和领域技能构成，采用分层证据匹配。

| 证据类型   | 命中权重 | 说明                             |
| ---------- | -------- | -------------------------------- |
| 直接命中   | 1.0      | 技能栏或项目中明确出现该技能     |
| 能力蕴含   | 0.7      | 已有具体技术实践可推导出上位能力 |
| 课程级证据 | 0.5      | 仅有课程学习记录，无实践证据     |
| 弱匹配     | 0.5      | 同技术大类或存在部分能力重叠     |
| 未命中     | 0        | 无对应能力证据                   |

基础计算：

```text
base_skill_score =
Σ（每项必备技能的最高证据权重）
÷ 必备技能数量
× 100
```

优先技能和领域技能仅作为加分项，不应掩盖必备技能缺口。

```text
skill_score = min(100, base_skill_score + preferred_bonus + domain_bonus)
```

若必备技能基础覆盖较弱，应限制加分项抬分，避免用户只命中 Python、深度学习等通用能力，却因优先项加分获得过高匹配分。

#### 通用基础能力处理

Python、PyTorch、Linux、Git、C++、SQL、Docker 等属于通用基础能力，不参与多 Query 召回构造，但在召回后参与技能校验。

- 命中 JD 必备基础技能：计入技能匹配；
- 未命中 JD 必备基础技能：降低技能分，并在报告中提示；
- 未命中 JD 优先基础技能：轻微扣分或仅提示；
- 对“Python / C++ 其一”这类替代要求，命中任一项即视为满足，不将其他候选技能作为缺口。

通用基础能力不能主导技能分。若 JD 核心要求为后训练、强化学习、推荐排序等方向能力，用户仅具备 Python、PyTorch、Linux 等基础技能时，不应获得高技能分。

------

### 3.2 项目分：LLM Rubric 评分

项目分仅依据用户简历中的项目、科研或实习经历评分，不将求职意向、课程名称或技能标签直接作为项目证据。

```text
project_score ∈ {0, 25, 50, 75, 100}
```

| 分数 | 含义                                            |
| ---- | ----------------------------------------------- |
| 0    | 无相关项目或经历证据                            |
| 25   | 仅有泛领域相关性，例如都属于 AI，但任务本身无关 |
| 50   | 存在部分任务、方法或技术重叠，但核心职责不同    |
| 75   | 项目任务、方法或业务场景高度相关                |
| 100  | 项目直接覆盖岗位的核心工作内容与关键技术        |

LLM 评分时必须遵守以下约束：

- 泛 AI 相关不等于岗位核心方向匹配；
- 应用层项目不等于训练层项目；
- RAG、Agent 等应用项目不能直接等价于预训练、后训练、RLHF 或强化学习项目；
- 若岗位核心是机器人控制、强化学习、轨迹规划、SLAM 或真机部署，用户没有对应项目证据时，只能给低分或中低分；
- 评分结果需返回匹配证据、主要缺口和简短理由。

------

## 4. 偏好加分

```text
rank_score = min(100, match_score + preference_bonus)
preference_bonus = direction_bonus + commute_bonus
```

偏好加分用于排序，不改变岗位基础适配度；岗位相关性应始终优先于用户软偏好。

建议总加分上限为 10 分：

| 偏好项       | 建议上限 |
| ------------ | -------- |
| 岗位方向偏好 | 6 分     |
| 通勤偏好     | 4 分     |

------

### 4.1 岗位方向偏好加分

岗位方向偏好包括搜广推、后训练、大模型应用等。

用户偏好和 JD 内容不采用字符串直接匹配，而是先映射到统一方向标签，再计算匹配程度。

示例标签：

```text
LLM_APPLICATION：
大模型应用工程、Agent、智能体、AI Agent、RAG、知识库问答、LLM 工程

POST_TRAINING：
后训练、SFT、DPO、RLHF、奖励模型、偏好优化、对齐训练

SEARCH_RECOMMENDATION：
搜广推、搜索、推荐、广告、排序、召回、CTR、CVR

REINFORCEMENT_LEARNING：
强化学习、RL、策略优化、价值函数、奖励建模
```

方向匹配分级：

| 匹配情况                 | 建议加分 |
| ------------------------ | -------- |
| 核心方向完全一致         | +6       |
| 同一上层方向、子方向接近 | +4       |
| 存在弱相关技术交集       | +1 至 +2 |
| 无明显关联               | +0       |

例如，用户偏好“大模型应用”，JD 为“Agent 开发工程师”，可映射到 `LLM_APPLICATION`，获得较高方向加分；用户偏好“后训练”，JD 仅为普通 RAG 应用开发，则不应给满分。

------

### 4.2 通勤偏好加分

通勤默认采用公交/地铁方式计算。

支持两类表达：

- “通勤小于 1 小时”；
- “距离更近优先”。

通勤仅作为排序偏好，不作为岗位过滤条件。

示例映射：

| 通勤情况               | 建议加分 |
| ---------------------- | -------- |
| 满足用户设定的通勤时长 | +4       |
| 接近目标时长           | +2       |
| 超出目标但可接受       | +1       |
| 明显超出目标           | +0       |

若用户仅要求“给出通勤时间和路线”，则不增加通勤偏好分，仅在推荐报告中展示公交/地铁通勤时间与路线。

------

## 5. 最终输出

每个岗位应同时输出：

```text
基础适配分：match_score
偏好加分：direction_bonus + commute_bonus
最终推荐分：rank_score
```

报告中应简要解释：

- 技能和项目匹配点；
- 主要技能缺口；
- 岗位方向偏好是否命中；
- 用户要求时的公交/地铁通勤时间与路线；
- 推荐结论。

推荐等级建议按最终推荐分划分：

| 最终推荐分 | 结论       |
| ---------- | ---------- |
| ≥ 75       | 推荐投递   |
| 50–74      | 可酌情投递 |
| < 50       | 暂不建议   |