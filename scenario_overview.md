# Resume2Job 场景概要

## 1. 当前范围

当前先固定两类核心入口：

1. 上传简历 → 推荐岗位并生成报告
2. 上传简历 + 岗位 JD → 适配性分析与技能差距报告

---

## 2. 全局业务规则

### 2.1 硬约束与软偏好

- **硬约束**：城市、学历与岗位类型在岗位召回前通过 `where` 过滤；岗位类型支持“实习、校招、社招”，默认按“实习”检索。若岗位库中没有满足全部硬约束的岗位，应明确说明，不自动放宽条件。
- **软偏好**：包括岗位方向偏好与通勤偏好。岗位方向如搜广推、后训练、大模型应用等，不作为召回过滤条件，而是在匹配评分中根据岗位方向与用户偏好的匹配程度增加偏好命中分；通勤偏好如“通勤小于 1 小时”或“距离更近优先”，默认按公交/地铁方式计算，并作为附加偏好参与评分，不压过岗位与简历的基础相关性。
- **排序原则**：岗位与简历的基础相关性优先于软偏好；通勤偏好不能压过岗位本身的适配度。

### 2.2 通勤规则

通勤只作为软偏好或报告补充信息，不作为硬过滤条件。

- 用户说“帮我找通勤小于 1 小时的岗位”：通勤作为软偏好，增加偏好命中分。
- 用户说“给出岗位推荐和公交地铁通勤时间”：在推荐报告中补充公交/地铁通勤时间与路线。
- 若需要实际通勤计算但用户未提供出发地，应触发追问。

### 2.3 多轮岗位指代规则

- 推荐报告中必须展示明确的 `job_id`。
- 用户后续分析、比较或追问具体岗位时，应使用 `job_id` 指定。

### 2.4 低置信与澄清机制

以下情况不强行执行，进入追问或澄清：

- 用户意图识别置信度低；
- 约束条件冲突或表达模糊；
- 用户要求 JD 分析但未提供 JD；
- 用户追问具体岗位但未提供有效 `job_id`；
- 用户要求通勤路线但未提供出发地；
- 用户请求过于模糊，例如“找好一点的岗位”。

---

## 3. 多 Query 召回与通用基础能力处理

岗位召回采用多 Query 策略：

Query1 更偏向用户的**方向标签**，用于表达其高层能力方向，如搜广推、后训练、大模型应用、强化学习等；

Query2 更偏向用户的**实践细节**，用于表达项目或实习中实际完成的任务、方法与技术实践，如 SFT、DPO、RAG、多路召回、自动化评测等。

Query3 用于表达用户**想做什么方向的岗位**，仅在存在明确偏好时生成。偏好来源优先级：当前用户输入中明确提出的方向偏好；简历中明确写出的求职意向；两者均不存在时，不生成 Query3。

允许两路 Query 存在少量必要的语义重叠，但避免整批关键词重复堆叠；当同一术语同时出现在技能和项目经历中时，优先在 Query1 保留方向性表达，在 Query2 保留更细粒度的实践描述。

Query3 不承载城市、学历、岗位类型等硬约束；这些条件应在召回前通过 `where` 过滤。Query3 只用于增强目标方向岗位的召回，最终排序仍由基础匹配分和方向偏好加分共同决定。

Python、PyTorch、Linux、Git、C++ 等语言、框架和开发环境属于通用基础能力，通常区分度较低，且许多岗位仅要求其中一项或若干项，因此不参与 Query 构造，以避免将召回结果过度拉向泛算法或泛开发岗位。它们在召回后结合 JD 的必备与优先要求进行匹配校验：命中必备要求时计入基础技能匹配；未命中必备要求时降低匹配分并在报告中提示关键缺口；未命中优先要求时轻微扣分或仅提示。对于“Python / C++ 其一”这类可替代要求，命中任一技能即可视为满足，不将其他候选技能视为缺口。

## 4. 场景 A：上传简历 → 推荐岗位报告

### 用户示例

> 上传简历，帮我找北京的搜广推实习，最好通勤在 1 小时内。

> 帮我推荐适合我的实习岗位。

### 系统主链路

```text
上传简历
→ 解析简历并生成用户画像
→ 城市等硬约束在召回前过滤
→ 搜广推方向、通勤偏好等作为软偏好
→ 混合召回、融合与匹配评分
→ 在总分中加入偏好命中分
→ 生成岗位推荐报告
```

### 约束处理

- “北京”属于硬约束，进入召回前 `where` 过滤。
- “搜广推”属于软偏好，在评分阶段增加偏好命中分。
- “通勤小于 1 小时”属于软偏好，在评分阶段增加通勤偏好分。
- 若用户要求展示通勤结果，则在报告生成阶段调用公交/地铁路线能力。

### 报告内容

- 推荐岗位列表及明确的 `job_id`；
- 每个岗位的总匹配分；
- 匹配依据，例如技能、项目、学历或方向匹配；
- 简要技能缺口；
- 软偏好命中说明，例如“岗位方向与搜广推偏好一致”；
- 用户明确要求时，补充公交/地铁通勤时间与路线；
- 若硬约束下无岗位，明确说明当前岗位库无匹配结果。

### 评分原则

```text
总分 = 基础匹配分 + 偏好命中分
```

其中：

- 基础匹配分：技能、项目、教育背景等简历—JD 适配；
- 偏好命中分：搜广推方向、通勤偏好等；
- 技能缺口是推荐报告的一部分，不构成独立任务。

---

## 5. 场景 B：上传简历 + JD → 适配性分析报告

### 用户示例

> 上传简历和岗位 JD，帮我看我是否适合。

> 这个岗位和我的经历匹配吗？我缺哪些技能？

### 系统主链路

```text
上传简历 + 提供岗位 JD
→ 解析简历并生成用户画像
→ 解析 JD 并生成岗位画像
→ 进行简历—JD 匹配评分
→ 汇总匹配点、技能缺口和关键风险要求
→ 生成适配性分析报告
```

### 报告内容

- 总体适配度；
- 已匹配的技能、项目经历与教育背景；
- 必备技能和优先技能中的主要缺口；
- 可能影响投递的要求，例如实习时长、学历、地点或特定经验；
- 是否建议投递及原因。

## 6. 多轮对话场景

多轮对话围绕“岗位推荐”和“JD 适配分析”两条核心路径展开。后续请求优先复用当前对话中已生成的推荐结果、岗位信息或 JD 分析结果；信息不足或指代不明确时，系统不进行猜测，而是主动追问澄清。

### 修改硬约束后重新推荐

用户可以在推荐结果基础上修改城市、学历或岗位类型等硬约束。

示例：

> 帮我找北京的搜广推实习。  
> 换成上海。  
> 只看校招。

系统沿用当前简历和软偏好，仅覆盖对应硬约束，并重新执行岗位召回、匹配评分与推荐报告生成。

由于硬约束会改变可选岗位范围，因此此类请求需要重新召回。

---

### 修改软偏好后重新排序

用户可以在已有推荐结果基础上补充或修改岗位方向、通勤等软偏好。

示例：

> 我更想看后训练方向。  
> 通勤尽量控制在一小时内。  
> 更偏搜广推一些。

系统保留当前硬约束和候选岗位范围，更新岗位方向偏好或通勤偏好，并依据新的偏好分重新排序。

岗位方向和通勤仅影响排序，不改变岗位本身的基础适配度，也不作为召回过滤条件。

---

### 换一批或展示更多岗位

首次推荐时，系统应保留除当前展示岗位外的其他相关候选岗位。

示例：

> 再来一批。  
> 多给几个岗位。

系统从当前候选结果中继续选择未展示的岗位生成推荐报告，不重新执行召回。

若当前候选结果不足以提供新的岗位，再提示用户修改城市、岗位类型或方向偏好后重新搜索。

---

### 基于 job_id 查看岗位详情

推荐报告中的每个岗位都应展示明确的 `job_id`。用户可通过 `job_id` 请求更完整的岗位适配分析。

示例：

> 详细分析 job_1024。  
> job_1024 适合我吗？

系统读取该岗位信息和当前简历画像，输出更完整的适配分析，包括：

- 技能匹配与主要技能缺口；
- 项目经历相关性；
- 学历和岗位类型资格情况；
- 方向偏好命中情况；
- 推荐结论与主要依据。

该场景不重新召回岗位。

---

### 查询岗位通勤信息/学习计划/面试辅助

用户可以查询推荐岗位或当前 JD 的公交、地铁通勤时间与路线。

示例：

> 查询 job_1024 从中关村过去的公交地铁通勤时间。  
> 帮我生成关于job_1024的学习计划/面试模拟题。

系统根据岗位办公地点和用户提供的出发地，返回公交或地铁通勤时间与路线。（高德API）

系统根据岗位推荐报告与技能差距分析给出学习计划。（LLM）

系统根据岗位JD信息给出面试辅助题。（LLM）

规则如下：

- 用户提供 `job_id` 时，查询对应岗位；
- 用户未提供 `job_id` 时，仅在当前对话存在唯一待分析 JD 的情况下使用该 JD；
- 未提供有效岗位信息或 JD 时，要求用户补充 `job_id` 或 JD；
- 未提供出发地时，要求用户补充出发地；
- 若用户此前表达了通勤偏好，可基于通勤结果更新该岗位的通勤偏好分。

---

## 7. 触发澄清的场景

以下情况不应强行推断，应主动追问：

| 用户输入                              | 系统行为                                     |
| ------------------------------------- | -------------------------------------------- |
| “第二个怎么样”                        | 请用户提供推荐报告中的 `job_id`              |
| “看看这个岗位”但未提供 JD 或 `job_id` | 请用户提供岗位 JD 或 `job_id`                |
| “算通勤”但未提供出发地                | 请用户提供出发地                             |
| “换一个方向”但未说明目标方向          | 请用户说明目标方向                           |
| “找更好的岗位”                        | 询问用户更看重匹配度、岗位方向还是通勤便利性 |
| 当前意图识别置信度较低                | 说明系统理解到的候选意图，并请求用户确认     |
